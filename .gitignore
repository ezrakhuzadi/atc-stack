.env
.env.*
!.env.example
*.log

# Local data mounts (OSM, Overpass DB, DEM tiles)
data/
osm/
overpass/
overpass-us/
terrain/

# OS-specific
.DS_Store

# Python caches (for mock-uss / terrain-api development)
__pycache__/
*.py[cod]

# Node caches (if you run tooling locally inside submodules)
node_modules/

# Generated TLS materials for local demo mode
certs/*
!certs/.gitkeep

# Timed audit clock state (local, per-machine)
.timed_audit_clock.json
